export const SWAP = {
  DEFAULT_SLIPPAGE_BPS: 100,
  MAX_SLIPPAGE_BPS: 5000,
  MIN_SLIPPAGE_BPS: 10,
  SLIPPAGE_PRESETS: [50, 100, 200] as const,
  DEADLINE_SECONDS: 600,
  APPROVAL_CONFIRMATIONS: 2,
  SWAP_CONFIRMATIONS: 2,
  QUOTE_DEBOUNCE_MS: 1000,
  QUOTE_REFRESH_MS: 10000,
  BALANCE_REFRESH_MS: 30000,
} as const;

export const TOKEN_DECIMALS: Record<string, number> = {
  ACES: 18,
  USDC: 6,
  USDT: 6,
  ETH: 18,
  WETH: 18,
  LAUNCHPAD: 18,
};

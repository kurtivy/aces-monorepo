{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 pnpm install --frozen-lockfile && PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 pnpm -C apps/backend run build"
  },
  "deploy": {
    "startCommand": "pnpm -C apps/backend start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

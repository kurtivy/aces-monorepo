'use client';

interface AdminButtonProps {
  onAdminDashboardClick?: () => void;
}

export function AdminButton() {
  // Admin button removed to keep admin system completely separate
  // Users can access admin dashboard directly via admin.aces.fun or localhost:3000/admin
  return null;
}

{
  "version": 2,
  "buildCommand": "pnpm vercel-build && cp -r dist/* api/ && mv api/vercel.js api/index.js",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": ".",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
